////586. Customer Placing the Largest Number of Orders

SELECT customer_number FROM orders//// Group by and oreder by special case
GROUP BY customer_number
ORDER BY COUNT(customer_number) DESC
LIMIT 1;
